# 수업내용정리
## 2026-09-10

### 1. Django 프로젝트 폴더 만들기
> 폴더 이름 Django_danggeun_market

### 2. Visual Studio Code 로 python 환경 세팅하기
> #### 1) 프로젝트 열기
>>  폴더열기 눌러서 위에서 만든 Django_danggeun_market 폴더 열기
>#### 2) 가상환경 세팅하기
>>  새 터미널 열기(Ctrl + Shift + `) 후 아래 명령 실행
>> ```bash
>> python -m venv .venv
>> ```
>> 
>> 그 다음 터미널에 아래 명령 실행
>> ```bash
>> .venv\Scripts\activate
>> ```
>>
> #### 3) 파이썬 인터프리터 설정
>>  가상환경 세팅 폴더안의 python 실행파일로 인터프리터 경로 설정 해주어야함
>>  - Ctrl + Shift + P 로 설정창 열기
>>  - python: Select Interpreter 클릭 후 "인터프리터 경로 입력" 을 누름
>>  - 프로젝트 폴더 안의 Scripts 폴더 안의 python.exe 선택
>>  - 제대로 되었는지 확인하려면 pip list 입력하면 pip만 떠야 정상
> #### 4) 관련 라이브러리 설치
>>  터미널 창에
>> ```bash
>> pip install "Django>=5.2,<5.3"
>> ```
>>   입력해서 장고 패키지 설치 (5.2 버전으로 진행)
>>
>> ```bash
>>  pip install psycopg[binary]
>>  pip install python-dotenv
>>  pip install dj-database-url
>>  pip install pillow
>>  pip install supabase
>>
>>  pip install channels
>>  pip install daphne
>>```
>>  (한 줄로 실행할 경우 아래 명령어로 설치)
>> ```bash
>>  pip install "Django>=5.2,<5.3" psycopg[binary] python-dotenv dj-database-url pillow supabase channels daphne
>>```
 ### 3. 장고 프로젝트 생성
>  터미널 창에서 django-admin startproject config .   를 입력 (주의, config 다음에 띄어쓰기하고 . 점 찍어야 함)
>  Django_danggeun_market 폴더 안에 Django 관련 폴더들이 생성됨
>
### 4. Django 앱 생성
>  터미널 창에서 python manage.py startapp market  를 입력
>  그러면 market 폴더가 생성됨
>("market" 이라는 앱이 생성된 것, Django 프로젝트 하나에 여러개의 앱이 있을 수 있음)
>
>config = 프로젝트 전체 설정,
>market = 중고거래 기능이 들어가는 실제 앱

### 5.앱 등록
>config/settings.py 열고 아래 내용에 "market"을 추가.
>```python
>INSTALLED_APPS = [
>    "django.contrib.admin",
>    "django.contrib.auth",
>    "django.contrib.contenttypes",
>    "django.contrib.sessions",
>    "django.contrib.messages",
>    "django.contrib.staticfiles",
>
>    "market",
>]
>```
>이 과정을 하지 않으면, Django가 우리가 만든 앱을 프로젝트 구성요소로 인식하지 못함

### 6. 한국 시간 설정
>config/settings.py 에서 해당 국가와 시간에 대한 설정을 아래처럼 변경
>```python
>LANGUAGE_CODE = "ko-kr"
> 
> TIME_ZONE = "Asia/Seoul"
> 
> USE_I18N = True
> 
> USE_TZ = True
> ```

### 7. Django 기본 DB 생성
> supabase 를 생성 전이므로, 기본 SQLite 를 잠깐 사용
> ```bash
> python manage.py migrate
> ```
> 이 작업을 진행하면 Django가 기본 회원, 관리자, 세션 기능을 사용하기 위해 필요한 테이블을 만든 것

### 8. 서버 실행
> 아래 명령을 실행하면 장고 서버가 로컬에서 실행됨
> ```python
> python manage.py runserver
> ```
> 브라우저에서 http://127.0.0.1:8000 으로 접속하면 장고의 기본 로켓 페이지가 보여야 정상

### 9. 페이지 만들기
> market/views.py 열어서 내용 변경 후 저장 (없으면 새로 생성)
> ```python
> from django.shortcuts import render
> 
> 
> def main(request):
>     return render(
>         request,
>         "market/main.html"
>     )
> ```
### 10. URL 설정하기 (앱 내부 설정)
> 위에서 main 함수 요청이 오면 market 폴더의 main.html 을 반환해주는 작업만 만들었으므로, 메인 URL에 접속시 main 함수가 실행되게끔 만들어주어야함
> -market 폴더 안에 urls.py 파일 생성
> -안에 아래 코드 입력 후 저장
> ```python
> from django.urls import path
> from . import views
> 
> 
> urlpatterns = [
>     path(
>         "",
>         views.main,
>         name="main"
>     ),
> ]
> ```
> 기본 주소로 접속시(http://127.0.0.1:8000) main.html을 화면을 보는 사람한테 보여주겠다는 작업임
> 

### 11. URL 설정하기 (프로젝트 설정)
> 위에서 작업한 내용은 market 앱과 관련된 주소들만 market/urls.py 에서 관리를 하는 것임.
> 따라서 장고 전체 프로젝트에서 url 세팅을 추가적으로 연결 해줘야함.
> config/urls.py 를 열고, 코드 내용을 변경
> ```python
> from django.contrib import admin
> from django.urls import path, include
> 
> 
> urlpatterns = [
>     path("admin/", admin.site.urls),
> 
>     path(
>         "",
>         include("market.urls")
>     ),
> ]
> ```
> - 흐름은 다음과 같음
> 
>   브라우저 → config/urls.py → market/urls.py → market/views.py  

### 12. templates 폴더 생성 및 main.html 작성
> 프로젝트 최상위에 templates 폴더 생성
> 그안에 market 폴더 생성
> 그안에 main.html 생성
> 
> templates/market/main.html
> ```html
> <!DOCTYPE html>
> <html lang="ko">
> 
> <head>
>     <meta charset="UTF-8">
> 
>     <title>
>         Danggeun Market
>     </title>
> </head>
> 
> <body>
> 
>     <h1>
>         Poly Market
>     </h1>
> 
>     <p>
>         Django로 만드는 중고거래 플랫폼입니다.
>     </p>
> 
> </body>
> 
> </html>
> ```
>
> 해당 코드까지 저장하고 http://127.0.0.1:8000 에 접속해서 방금 업로드한 main.html 내용이 나오는지까지 확인


### 13 프로젝트 Github에 업로드
> 장고 프로젝트 초기세팅이 완료되었으므로, github에 프로젝트 폴더 업로드해야됨.
> 
> #### 1) .gitignore 만들기
>>   github에 올리면 안되는 내용의 파일들을 적어야함
>>   최상위 폴더에서 .gitignore 파일을 만들고 아내 내용을 적어서 저장.
>> 
>>   ```
>> # Python
>> __pycache__/
>> *.pyc
>> *.pyo
>> 
>> # Virtual Environment
>> venv/
>> .venv/
>> 
>> # Environment Variables
>> .env
>> 
>> # Django
>> db.sqlite3
>> media/
>> 
>> # VS Code
>> .vscode/
>> 
>> # OS
>> .DS_Store
>> Thumbs.db
>> ```
>
> #### 2) Git 저장소 초기화
>> ```git
>> git init
>> ```
>> 해당 명령을 git bash에 입력하면 프로젝트 폴더에 .git 폴더가 생성됨
>> 
> #### 3) Git 사용자 이름과 이메일 설정
>>   처음 Git을 설치한 PC인 경우 사용자 정보 입력해줘야됨
>> ```git
>> git config --global user.name "GitHub이름"
>> git config --global user.email "example@gmail.com"
>> ```
>> 
>> 사용자 이름 및 이메일 설정 확인
>> ```git
>> git config --global --list
>> ```
>> 
>> 
> #### 4) GitHub 에서 Respository 만들기
>> 깃허브에 로그인하고 새 Respository 생성 눌러서 저장소 이름을
>> 
>> Django_danggeun_market
>> 
>> 로 생성
>> 
>> - 되도록 빈 저장소로 만드는 것을 추천.
>> - README 파일과 .gitignore 파일 만들면 첫 push가 어려워짐
>>   
> #### 5) 현재 파일 상태 확인
>> ```git
>> git status
>> ```
>> 입력하면 수집된 파일 확인 가능
>> 
>> ```
>> venv/
>> .env
>> db.sqlite3
>> ```
>> 파일들이 목록에 없어야 정상
>> 
> #### 6) 전체 파일 stage에 추가
>> ```git
>> git add .
>> ```
>> 해당 명령은 현재 프로젝트의 변경 파일을 전부 추가한다는 뜻
>> 
>> 잘 추가되었는지 확인하려면
>> ```git
>> git status
>> ```
>> 입력하면 됨
> #### 7) 첫 commit
>> ```git
>> git commit -m "Initial Django project"
>> ```
>> 커밋 메세지는 "현재 작업 상태를 하나의 버전으로 저장" 하는 것임
>> 
>> 코멘트를 달아서 버전 정보에 대해서 확인할 수 있게 하므로 알기 쉽게 설명을 함께 달아주어야함.
>> 
>> 되도록 영어로 하는 것을 추천, 모르겠으면 ChatGPT에 뭐로 저장하면 좋을지 물어보고 저장할것.
>> 
>> 그리고 조금 변경할 때마다 commit 하면 나중에 보기 안 좋음. 의미있는 변경만 commit 하는 것을 추천
>> 
> #### 8) 기본 브랜치를 main으로 변경
>> ```git
>> git branch -M main
>> ```
>> 을 입력해서 git 에서 기본 브랜치가 master 인데, 이것을 main 으로 이름 변경 (큰 상관은 없음)
>> 
> #### 9) Github 저장소 연결
>> 자신의 깃허브 주소를 복사하고, git bash 명령을 다음처럼 입력
>> ```git
>> git remote add origin https://github.com/username/Django_danggeun_market.git
>> ```
>> 
>> 잘 연결되었는지 확인
>> ```git
>> git remote -v
>> ```
>> 
>> 잘 연결되었으면 아래처럼 결과가 뜸
>> ```git
>> origin  https://github.com/username/Django_danggeun_market.git (fetch)
>> origin  https://github.com/username/Django_danggeun_market.git (push)
>> ```
>> 
> #### 10) Github에 첫 Push
>> 처음 github에 프로젝트를 올릴 때에는 -u origin main 을 붙여서 push 명령을 내려야함
>> ```git
>> git push -u origin main
>> ```
>> 
>> 그 이후부터는 그냥
>> ```git
>> git push
>> ```
>> 로만 깃허브에 파일 업로드를 하게됨
>> 
>> 만약 깃허브에 readme.md 같은게 만들어져있다면 git push가 안될 수 있음
>> 
>> 이는 깃허브 레파지토리와 내 로컬 pc의 프로젝트 버전이 달라서 생긴 문제로,
>> 
>> 강제로 업로드 하는 방법이 있음
>> ```git
>> git push --force
>> ```
>> 
>> 이 방법은 잘 안될 때만 이용하고, 다음부터는 프로젝트를 pull 로 받아와서 정상적으로 github를 유지하는 것이 좋음
>









